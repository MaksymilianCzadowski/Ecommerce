{% extends 'base.html.twig' %}

{% block title %}Hello ShopController!{% endblock %}

{% block body %}
<<<<<<< HEAD
    <section class="row product">
        {% for produit in produits %}
            
        <article class="col-md-6 mx-auto text-center border border-dark mb-3 p-3 mt-3 mr-1" >
            <div class="content" href="{{ path('app_detail', {'id': produit.id}) }}">
                <img src="{{ produit.image }}"
            </div>
            <h4>{{ produit.nom }}</h4>
            <div class="metadata">
            {{  produit.prix}}
=======
<section>
    <div class="container py-5">
        <div class="row">
            {% for produit in produits %}
            <div class="col-md-12 col-lg-4 mb-4 mb-lg-0">
                <div class="card text-black">
                    <img src="{{ produit.image }}" class="card-img-top" />
                    <div class="card-body">
                        <div class="text-center mt-1">
                            <h4 class="card-title">{{ produit.nom }}</h4>
                            <h6 class="text-primary mb-1 pb-3">{{ produit.prix }}€</h6>
                        </div>

                        <div class="d-flex flex-row">
                            <button type="button" class="btn btn-primary flex-fill me-1" data-mdb-ripple-color="dark" href="{{ path('app_detail', {'id': produit.id}) }}">
                                En savoir plus
                            </button>
                            <button type="button" class="btn btn-success flex-fill ms-1">Ajouter au panier</button>
                        </div>
                    </div>
                </div>
>>>>>>> a8f6b5b9df3d12da59de679b2e0837b563e0c9b4
            </div>


            {% endfor %}

            {% endblock %}